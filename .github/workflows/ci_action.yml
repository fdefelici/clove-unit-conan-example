name: CI Action

on:
  push:
      branches: 
        - '**'
        - '!master'
      paths-ignore: 
        - '**/*.md'
        - '**/*.png'
  pull_request:
      branches:
        - '**'
jobs:
  build_macos:
    name: Build macos_apple-clang
    #runs-on: macos-latest
    runs-on: macos-14
    defaults:
      run:
        shell: bash
    env:
      BUILD_DIR: ${{ github.workspace }}/build
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: List Workspace
        run:  ls -la ${{ github.workspace }}
      - name: Prepare
        run: |
          pip install conan==2.3.1
          conan profile detect --force
      - name: Compile
        run: |
          echo "== CPU VERSION =="
          sysctl -n machdep.cpu.brand_string
          echo "== CONAN VERSION =="
          conan --version
          echo "== COMPILER VERSION =="
          clang --version
          echo "== CMAKE VERSION =="
          cmake --version
          echo "== CMAKE CONFIGURE =="
          conan install . --settings=build_type=Debug
          cmake --preset conan-debug
          cmake --build --preset conan-debug
      - name: List Build
        run: |
          ls -la ${{env.BUILD_DIR}}/Debug
          file ${{env.BUILD_DIR}}/Debug/all_tests
      - name: Test
        run: ${{env.BUILD_DIR}}/Debug/all_tests -x

